# First Git Project

### Hello from github

```
some code
```

## Basic GIT commands

```declarative
    - git status
    - git add .
    - git add ${path/to/file}
    - git commit -m 'some messsage'
    - git checkout -b ${branch-name}
    - git push origin ${branch-name}
```